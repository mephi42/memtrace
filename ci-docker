#!/bin/sh
set -e -u -x

cd "$(dirname "$0")"
version=2020-10-07-976386e5b
docker run \
    --interactive \
    --tty \
    --rm \
    --volume "$PWD:$PWD" \
    --workdir "$PWD" \
    mephi42/memtrace-build-manylinux2014_"$(uname -m)":"$version" \
    ./ci
